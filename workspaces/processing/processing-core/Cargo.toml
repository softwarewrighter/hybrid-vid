[package]
name = "processing-core"
version = "0.1.0"
edition = "2024"

[lib]
name = "processing_core"
path = "src/lib.rs"

[features]
default = []
wasm = []

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
log = "0.4"
